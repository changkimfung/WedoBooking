# 仓储中台 · 预约送仓管理需求文档

## 1. 文档基础信息

### 1.1 版本记录

| 时间 | 版本 | 修改人 | 变更内容 |
| --- | --- | --- | --- |
| 2026-06-01 | v1.0 | — | 初版，基于当前原型实现整理 |

### 1.2 文档范围

| 项目 | 内容 |
| --- | --- |
| 模块名称 | 仓储中台 · 预约送仓管理 |
| 适用端 | 运德仓储中台 Web（`/wh/`） |
| 页面范围 | 预约列表、预约详情、新建入库单预约 |
| 不在范围 | 海外仓审核操作、PDA 到仓登记、货代端协作页（仅描述衔接关系）；客户前台自助创建（见《客户前台预约送仓需求文档》） |

### 1.3 术语表

| 术语 | 说明 |
| --- | --- |
| 预约方 | 发起预约的主体：客户编号（如 CN0000438）或「运德船务」 |
| 预约渠道 | `bookChannel`：customer（客户/代客）/ shipping（运德船务） |
| 预约单号 | 系统正式单号，格式如 YY20260528102 |
| 送仓码 | 6 位业务码，用于生成货代端预约链接 |
| 送仓总箱数 | 预约阶段申报的预计送仓箱数 |
| 收货总箱数 | PDA 到仓登记的实际箱数，列表/详情只读展示 |
| 装柜清单 | 船务装柜数据，可一键带入创建预约 |
| W.BOL | 预约确认后的入仓凭证，由海外仓/PDA 流程生成，中台只读展示链接 |

---

## 2. 需求背景

### 2.1 背景描述

预约送仓链路涉及客户前台、货代端、海外仓、PDA 等多端协作。仓储中台作为**内部运营枢纽**，需要：

1. **全局可视**：跨客户查看所有预约单及状态，支撑客服答疑、异常跟进；
2. **代客/代船务建单**：客户无法自助或船务头程整柜场景，由中台直接创建并提交仓库审核；
3. **装柜联动**：从装柜清单带入集装箱与票号，减少重复录入；
4. **只读追溯**：查看仓库审核结果、到仓收货、协商日志，不在中台执行审核（审核归属海外仓）。

### 2.2 需求列表

| 编号 | 用户诉求 | 提出人/部门 |
| --- | --- | --- |
| R-01 | 按客户/单号/仓库/状态筛选全量预约单 | 客服/仓储运营 |
| R-02 | 代客户或运德船务创建预约并直接提交仓库 | 船务/客服 |
| R-03 | 从装柜清单预填整柜预约信息 | 船务 |
| R-04 | 查看预约全貌（含仓库确认、到仓收货、日志） | 客服/运营 |
| R-05 | 获取货代端预约链接转发协作 | 客服 |
| R-06 | 监控送仓箱数与收货箱数差异 | 仓储/财务 |

### 2.3 核心目标

**作为仓储中台运营人员，我希望集中查看与管理全渠道预约单，并在必要时代客建单，以便提升协同效率、减少线下 Excel 传递。**

核心指标：中台代建单占比、代建单一次提交成功率、客服查询预约详情平均耗时。

---

## 3. 功能总览

### 3.1 业务流程



**仓储中台职责边界：**

| 负责 | 不负责 |
| --- | --- |
| 全量预约单列表查询与详情只读展示 | 海外仓审核（分配时段/驳回） |
| 代客户/运德船务新建并提交预约 | 货代端期望日期、备注、备用邮箱维护 |
| 从装柜清单预填创建页 | PDA 扫码到仓登记 |
| 展示预约链接供转发 | 客户前台草稿/废弃/接受时段等客户侧操作 |
| 展示仓库确认、到仓收货、操作日志 | 修改已提交预约的基础信息 |

### 3.2 功能清单

| 编号 | 功能模块 | 功能简述 |
| --- | --- | --- |
| F-01 | 预约列表 | 多条件筛选、分页、跳转详情 |
| F-02 | 新建入库单预约 | 代客/船务建单、关联入库单、提交至仓库待确认 |
| F-03 | 装柜清单联动 | URL 参数预填整柜预约与票号明细 |
| F-04 | 预约详情 | 预约信息、仓库确认、到仓收货、货物明细、日志 |
| F-05 | 预约链接 | 详情展示货代端链接，供客服转发 |

### 3.3 与各端差异对照

| 维度 | 客户前台 | 货代端 | 仓储中台 | 海外仓 |
| --- | --- | --- | --- | --- |
| 数据范围 | 当前客户，排除船务渠道 | 凭送仓码单条 | **全量** | 仓库待确认及后续状态 |
| 创建预约 | 有（草稿/提交→待预约） | 无 | **有（直接→仓库待确认）** | 无 |
| 预约方 | 固定当前客户 | — | 用户编号 / 运德船务 | — |
| 审核 | 无 | 无 | 无 | **有** |
| 状态操作 | 提交/取消/接受时段等 | 提交/确认/取消等 | **仅创建提交，详情只读** | 审核/PDA |
| 列表 Tab | 多状态 Tab | 无列表 | 无 Tab，下拉筛状态 | 状态 Tab |

---

## 4. 功能详细设计

### 4.1 预约列表（F-01）

#### A. 用户故事

作为仓储中台运营，我想要按客户、单号、仓库等条件筛选预约单，以便快速定位目标单据并查看详情。

#### B. 用户旅程

入口（单据管理 → 预约送仓管理）→ 设置筛选条件 → 点击「查询」→ 浏览列表 → 点击「详情」进入详情页；或点击「新建入库单预约」进入创建页。

#### C. 交互与界面

| 区域 | 说明 |
| --- | --- |
| 页眉 | 仓储中台统一导航；当前二级菜单高亮「预约送仓管理」 |
| 筛选栏 | 两行表单：客户编号、预约单号、预约仓库、送仓类型、状态；查询/重置 |
| 工具栏 | 右上角「新建入库单预约」主按钮 |
| 列表区 | 表格 + 分页（每页 10 条）+ 结果条数提示 |
| 权限 | 登录中台账号；**展示全量预约单**（含客户渠道与船务渠道） |

**列表字段（不展示总体积、总重量）：**

| 列名 | 说明 |
| --- | --- |
| 预约方 | 客户编号或「运德船务」 |
| 预约仓库 | 目的仓 |
| 预约单号 | 无则「-」 |
| 送仓码 | 无则「-」 |
| 状态 | 原状态文案 |
| 送仓类型 | 整柜 / 散货 |
| 送仓总箱数 | 申报箱数 |
| 收货总箱数 | PDA 写入；未到仓为「-」 |
| 是否打托 | 是 / 否 |
| 送仓总托数 | 未打托为 0 |
| 期望送仓日期 | 货代端填写后回显 |
| 仓库确认时段 | 审核通过后回显 |
| 实际送仓时间 | 到仓后回显 |
| 提交时间 | 首次提交时间 |
| 操作 | 详情 |

#### D. 字段与逻辑

**1. 筛选栏**

| 筛选元素 | 组件类型 | 默认值 | 逻辑/备注 |
| --- | --- | --- | --- |
| 客户编号 | 文本输入 | 空 | 模糊匹配（包含即命中）；匹配 `customerCode` |
| 预约单号 | 文本输入 | 空 | 模糊匹配 |
| 预约仓库 | 下拉框 | 全部 | 选项来自系统仓库列表 |
| 送仓类型 | 下拉框 | 全部 | 整柜 / 散货 |
| 状态 | 下拉框 | 全部 | 选项来自当前数据已有状态去重（含待提交、待预约等客户侧状态） |

**2. 分页**

| 规则 | 说明 |
| --- | --- |
| 每页条数 | 10 |
| 翻页 | 上一页 / 页码 / 下一页；筛选变更时重置为第 1 页 |
| 空数据 | 表格居中展示「暂无数据」 |

**3. 行内操作**

| 操作 | 行为 |
| --- | --- |
| 详情 | 跳转 `deliveryAppointmentDetail.html?id={预约单id}` |

**4. 数据同步**

| 场景 | 处理 |
| --- | --- |
| 其他 Tab/端修改预约数据 | 监听 storage 变更自动刷新列表 |

---

### 4.2 新建入库单预约（F-02 / F-03）

#### A. 用户故事

作为船务/客服，我想要代客户或运德船务创建送仓预约并关联入库单，以便跳过客户自助流程、直接进入仓库审核队列。

#### B. 用户旅程

**常规路径：** 列表 → 新建 → 选择目的仓与预约方 → 填写箱托信息 → 添加入库单 → 提交 → 返回列表（状态为仓库待确认）。

**装柜联动路径：** 装柜清单 → 「创建预约」→ 创建页自动预填集装箱、柜型、票号明细 → 核对后提交。

#### C. 交互与界面

| 区域 | 说明 |
| --- | --- |
| 预约基础信息 | 目的仓、预约方、用户编号、送仓类型、箱托、整柜字段 |
| 联系与协作 | 联系邮箱、货代公司（均选填） |
| 货物明细 | 已选入库单表格 + 「添加入库单」按钮 |
| 入库单选择器 | 模态框：按入库单号/集装箱号/目的仓查询勾选 |
| 底部按钮 | 提交预约、取消（返回列表） |

**联动规则：**

| 条件 | 界面行为 |
| --- | --- |
| 预约方 = 运德船务 | 用户编号固定为「运德船务」且禁用 |
| 预约方 = 用户编号 | 用户编号可编辑，默认演示客户 CN0000438 |
| 送仓类型 = 整柜 | 显示集装箱号、柜型，必填 |
| 送仓类型 = 散货 | 隐藏集装箱号、柜型 |
| 是否打托 = 是 | 显示送仓总托数，必填正整数 |
| 是否打托 = 否 | 隐藏送仓总托数，保存时置空 |
| 变更目的仓且已有明细 | 清空已选入库单 |

#### D. 字段与逻辑

**1. 预约基础信息**

| 字段 | 组件 | 必填 | 逻辑/备注 |
| --- | --- | --- | --- |
| 目的仓 | 下拉 | 是 | 系统仓库列表 |
| 预约方 | 下拉 | 是 | 用户编号 / 运德船务 |
| 用户编号 | 文本 | 预约方=用户编号时必填 | 写入 `customerCode` |
| 送仓类型 | 下拉 | 是 | 散货 / 整柜，默认散货 |
| 送仓总箱数 | 数字 | 是 | 正整数 ≥1；添加入库单后可自动合计 |
| 是否打托 | 下拉 | 是 | 是 / 否，默认否 |
| 送仓总托数 | 数字 | 打托时必填 | 正整数 ≥1 |
| 集装箱号 | 文本 | 整柜必填 | — |
| 柜型 | 文本 | 整柜必填 | 占位「如 40HQ」 |
| 联系邮箱 | 邮箱 | 否 | 有值时写入 `emails[0]` |
| 货代公司 | 文本 | 否 | — |

**2. 入库单选择规则**

| 预约方 | 可添加入库单条件 |
| --- | --- |
| 用户编号（customer） | 状态=运输在途；发货方式=客户自发头程；收货仓=目的仓 |
| 运德船务（shipping） | 状态=运输在途；收货仓=目的仓；**不限制发货方式** |

**入库单选择器：**

| 筛选项 | 逻辑 |
| --- | --- |
| 入库单号 | 模糊匹配 |
| 集装箱号 | 从装柜清单查票号；填写时按集装箱维度检索 |
| 目的仓 | 可选 FBA（cn 开头目的仓统一展示为 FBA） |

| 校验 | 错误提示 |
| --- | --- |
| 须先选目的仓（常规添加入库单） | 请先选择目的仓 |
| 勾选目的仓不一致 | 所选入库单目的仓不一致，请按同一目的仓勾选 |
| 至少 1 条明细 | 请至少添加一条入库单 |
| 已添加不可重复勾选 | 选择器内已添加行禁用勾选 |

**3. 货物明细行**

| 字段 | 说明 |
| --- | --- |
| 入库单号 ~ 订单箱数 | 只读 |
| 送仓箱数 | 可编辑；默认=订单箱数；1 ≤ 送仓箱数 ≤ 订单箱数 |
| 操作 | 移除 |

**4. 送仓总箱数一致性（customer 渠道）**

| 规则 | 说明 |
| --- | --- |
| 适用 | `bookChannel = customer` |
| 公式 | 送仓总箱数 = Σ 各明细行送仓箱数 |
| 不一致 | 模态框展示对比摘要，按钮「我知道了」阻断提交 |
| 船务渠道 | `bookChannel = shipping` **豁免**一致性校验 |

**5. 提交逻辑**

| 项目 | 规则 |
| --- | --- |
| 目标状态 | **仓库待确认**（不经待预约/待提交） |
| 生成字段 | 预约单号、送仓码、提交时间、操作日志 |
| bookChannel | 用户编号 → customer；运德船务 → shipping |
| 期望送仓日期 | 中台创建时不填，留空；由货代端后续补充（若需货代协作） |
| 装柜来源 | 写入 `sourceType=containerLoading`、`sourceContainerNo` 等溯源字段 |
| 成功提示 | 「预约单已创建，状态为仓库待确认」→ 跳转列表 |

**6. 装柜清单预填（F-03）**

| URL 参数 | 行为 |
| --- | --- |
| `source=containerLoading` | 启用装柜预填模式 |
| `containerNo={箱号}` | 匹配装柜清单，带入集装箱号、柜型、实装箱数、票号明细 |
| 预填默认值 | 预约方=运德船务；送仓类型=整柜；目的仓按票号/目的港推断 |

| 目的仓推断规则 | 说明 |
| --- | --- |
| 票号目的仓 cn 开头 | 展示为 FBA |
| 目的港含 SAVANNAH / NEW YORK | 美东仓 |
| 其他默认 | 深圳A仓（演示数据） |

---

### 4.3 预约详情（F-04 / F-05）

#### A. 用户故事

作为客服，我想要查看预约单完整信息、仓库审核结果与到仓收货情况，以便答复客户咨询并转发货代协作链接。

#### B. 用户旅程

列表点「详情」→ 查看预约信息 / 仓库确认 / 到仓收货 → 切换「货物明细 / 日志明细」Tab → 复制预约链接 → 返回列表。

#### C. 交互与界面

| 区域 | 说明 |
| --- | --- |
| 顶部 | 返回列表链接；客户编号强调展示 |
| 预约信息 | 只读字段网格 |
| 仓库确认信息 | 确认地址、确认时段、审核备注 |
| 到仓收货信息 | 实际到仓时间、收货总箱/托数、到仓拍照缩略图 |
| Tab | 货物明细（默认）、日志明细 |
| 操作 | **无状态变更按钮**（只读页） |

#### D. 字段与逻辑

**1. 预约信息**

| 字段 | 展示规则 |
| --- | --- |
| 预约仓库、预约单号、送仓码、状态、送仓类型 | 始终展示 |
| 送仓总箱数、是否打托、送仓总托数 | 始终展示；未打托托数为 0 |
| 集装箱号、柜型 | **仅整柜**展示 |
| 货代公司、联系邮箱 | — |
| 期望送仓日期、货代备注 | 货代端填写后回显 |
| 提交时间 | — |
| 预约链接 | 可点击新窗口打开货代端 `/fg/index.html?code={送仓码}` |

**不展示：** 总体积、总重量。

**2. 仓库确认信息**

| 字段 | 展示规则 |
| --- | --- |
| 仓库确认地址 | 未审核时「待确认」 |
| 仓库确认时段 | 未审核时「待确认」 |
| 仓库审核备注 | 无内容「-」 |

**3. 到仓收货信息**

| 字段 | 展示规则 |
| --- | --- |
| 实际到仓时间 | 美仓追加时区标注，如 `(US/Pacific)` |
| 收货总箱数 / 收货总托数 | PDA 写入；未到仓「-」 |
| 到仓拍照 | 缩略图列表；无则占位 |

**4. 货物明细 Tab**

| 列 | 说明 |
| --- | --- |
| 入库单号、订单状态、收货仓、发货方式 | 只读 |
| 订单箱数、送仓箱数、收货箱数 | 收货箱数 PDA 回写 |
| 提交时间 | 入库单创建时间 |
| 空数据 | 「无关联入库单」 |

**5. 日志明细 Tab**

| 元素 | 说明 |
| --- | --- |
| 时间 + 操作人 + 内容 | 与客户前台、海外仓、货代端同源 `operationLogs` |
| 空数据 | 「暂无日志」 |

**6. 异常处理**

| 场景 | 处理 |
| --- | --- |
| URL 无 id 或预约不存在 | 展示「预约单不存在」+ 返回列表 |

---

## 5. 与上下游模块衔接

| 模块 | 衔接说明 |
| --- | --- |
| 客户前台 | 客户自建单经货代端提交后，中台列表可见；客户渠道单 `bookChannel=customer` |
| 货代端 | 详情展示预约链接；货代补充期望日期/备注后状态仍为仓库待确认 |
| 海外仓 | 中台提交 → 仓库待确认 → 海外仓审核 → 客户待确认 / 预约失败 |
| 装柜清单 | ERP 装柜清单跳转创建页并预填；`sourceType` 溯源 |
| 邮件通知 | 中台直接提交至仓库待确认时，是否触发「预约申请已收到」见待确认事项 |
| PDA | 待送仓 → PDA 到仓 → 已送仓；收货数据回写中台列表/详情 |

---

## 6. 规则与约束

### 6.1 权限与访问

| 规则 | 说明 |
| --- | --- |
| 登录访问 | 需仓储中台账号登录（原型为演示用户） |
| 数据范围 | 全量预约单，含 customer 与 shipping 渠道 |
| 操作权限 | 创建 + 只读查看；不可在中台执行审核或代货代提交 |

### 6.2 业务规则汇总

| 编号 | 规则 |
| --- | --- |
| BR-01 | 中台新建提交：直接 → **仓库待确认**，不经过待预约 |
| BR-02 | 船务渠道（shipping）豁免送仓总箱数与明细一致性校验 |
| BR-03 | 客户渠道（customer）提交须满足箱数一致性 |
| BR-04 | 船务渠道入库单不限制「客户自发头程」发货方式 |
| BR-05 | 预约单号、送仓码在提交时自动生成且唯一 |
| BR-06 | 列表、详情不展示总体积、总重量 |
| BR-07 | 预约方展示：`bookChannel=customer` → 客户编号；`shipping` → 「运德船务」 |
| BR-08 | 详情页不提供状态变更操作，审核归属海外仓 |

### 6.3 非功能要求（原型阶段）

| 分类 | 要求 |
| --- | --- |
| UI 风格 | 仓储中台 legacy 表单/表格风格 |
| 持久化 | sessionStorage + mock 源文件（需 dev 服务） |
| 响应式 | 面向 PC 浏览器 |
| 多 Tab 同步 | 列表/详情监听 storage 变更自动刷新 |

---

## 7. 附录

### 7.1 状态—中台能力矩阵

| 状态 | 列表可见 | 详情可见 | 中台可创建进入 | 中台可操作 |
| --- | --- | --- | --- | --- |
| 待提交 | ✓ | ✓ | — | — |
| 待预约 | ✓ | ✓ | — | — |
| 仓库待确认 | ✓ | ✓ | ✓（新建直达） | — |
| 客户待确认 | ✓ | ✓ | — | — |
| 待送仓 | ✓ | ✓ | — | — |
| 已送仓 | ✓ | ✓ | — | — |
| 已超时 | ✓ | ✓ | — | — |
| 预约失败 | ✓ | ✓ | — | — |
| 已废弃 | ✓ | ✓ | — | — |

### 7.2 创建渠道对比

| 维度 | 客户前台创建 | 中台代客创建 | 中台船务创建 |
| --- | --- | --- | --- |
| bookChannel | customer | customer | shipping |
| 提交后状态 | 待预约 | 仓库待确认 | 仓库待确认 |
| 箱数一致性 | 强制 | 强制 | 豁免 |
| 入库单发货方式 | 客户自发头程 | 客户自发头程 | 不限 |
| 装柜预填 | 无 | 支持 | 支持 |
| 客户列表可见 | 是 | 是 | **否**（船务单不在客户前台列表） |

### 7.3 相关文档

| 文档 | 说明 |
| --- | --- |
| [`客户前台预约送仓需求文档.md`](./客户前台预约送仓需求文档.md) | 客户自助创建与状态操作 |
| [`货代端预约送仓需求文档.md`](./货代端预约送仓需求文档.md) | 货代协作与时段确认 |
| [`预约送仓邮件模板需求文档.md`](./预约送仓邮件模板需求文档.md) | 邮件触发与模板 |

---

## 8. 待确认事项

| 编号 | 问题 | 建议 |
| --- | --- | --- |
| OQ-01 | 中台直接提交至仓库待确认，是否跳过货代端？期望送仓日期由谁填写？ | 当前中台创建不填期望日期；若需货代补充，应先进「待预约」或创建后主动转发链接 |
| OQ-02 | 中台代建单是否触发「预约申请已收到」邮件？ | 邮件文档定义为货代/客户提交 → 仓库待确认；中台代建是否等同需业务确认 |
| OQ-03 | 中台是否需要「导出 Excel」？ | 原型未实现；客服对账场景可能需补充 |
| OQ-04 | 中台详情是否需要「代客取消/废弃」？ | 当前只读；若客服需代操作，需定义权限与状态限制 |
| OQ-05 | 船务创建单是否在客户前台任何入口可见？ | 当前 `bookChannel=shipping` 对客户列表不可见 |
| OQ-06 | 列表是否需要状态 Tab（如海外仓）？ | 当前仅下拉筛选；可按运营习惯评估是否加 Tab |
